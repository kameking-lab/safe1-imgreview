# RULES_EVENT23 — 25枚を1枚ずつOpusが読込分析し「専用プロンプト個別設計」→Gemini生成（やり直し版・全自動・無人継続）

## ■目的 / 前回までの失敗の是正
- これまで（v20〜v22）は汎用プロンプト＋元画像を渡すだけで、1枚ごとの作り込みが無く品質が低かった。**今回はそれを禁止する。**
- 各元絵について、Opus最上級モデル（claude-opus-4-8＝ワーカー自身）が**元画像を実際に Read で読み込み**、何が描かれているか（機械の種類・人の姿勢/動作/人数・事故の状況・なぜ事故が起きたか＝原因）を1枚ずつ丁寧に分析し、その元絵**専用**の生成プロンプトを個別に書く。**テンプレの使い回し禁止。**

## ■【最重要】汎用テンプレ禁止・1枚ずつ専用プロンプトを丁寧に設計（時間をかけてよい）
各元絵について、以下を満たす専用プロンプトを個別作成（日本語＋英語）し `prompts_v23/{番号}.txt` に全文保存:
1. 【原因への忠実性】元絵の事故の原因・きっかけ（不安全姿勢・不安全状態）を忠実に再現（手すりに足をかける/身を乗り出す/フック未接続/真下に人がいるのに下降/積みすぎ/無理な姿勢 等、元絵の原因をそのまま守る）。原因が見る人に分かること。
2. 【姿勢・状況への忠実性】元絵の作業員の姿勢・動作・人数、機械との位置関係を忠実に保つ。
3. 【事故の瞬間】原因姿勢から事故が起きる「まさにその瞬間」を描く（落ちきった後でない）。
4. 【物理的忠実性】重力・重心・接触・機械構造を物理的に正しく。ありえない浮き方/構造破綻をしない。重心がどう崩れて事故になるかが整合すること。
5. 【展示会場屋内への置換】舞台＝日本の展示会場・ブース設営現場の屋内に統一。屋外なら屋内会場へ置換。周囲＝組みかけ展示ブース/什器/看板パネル/搬入物。アイ型鉄骨・H鋼等の構造物→展示会場のトラス支柱に置換。床の段差・縁石→電線コードカバー(ケーブルプロテクター)に置換。屋外要素(地面/外壁/空)→屋内会場に置換。
6. 【シザース式統一】高所作業車は必ずシザースリフト（垂直昇降式）。ブーム式/トラック式が元絵でもシザース式に置換。
7. 【画風】高品質・臨場感のある安全教育用イラスト（陰影・立体感のあるクリーンな作画）。タッチを全25枚で統一。
8. 【禁止】矢印を入れない。文字・キャプション・吹き出し・記号も入れない（画像内テキストなし）。流血なし。実在ロゴ/ブランドなし。

## ■安全（厳守）
- 削除/上書き禁止（追加は必ず新ファイル名）。既存成果物（collect_aerial2/・gen_event_v20〜22/・review_* 等）は非破壊。
- Chrome を kill しない。taskkill しない。rm/Remove-Item/del/move を使わない。
- **OpenAI は使わない**（Google gemini-3-pro-image-preview のみ）。
- APIキー/認証値は **値を出力しない**（`.env` から読む。出す場合は末尾4桁マスクのみ）。
- 時刻は `powershell.exe -Command "Get-Date"`。git push は origin master(=main) に append-only。競合は `git pull --rebase` 後再push。
- 着手前に `gen_event_v23/` の既存を確認し**未生成分のみ**（出力先が既にあれば skip＝再開安全）。捏造禁止。

## ■元絵25番号 / 分析
1,2,3,45,64,91,93,94,97,100,104,112,114,137,138,196,215,372,379,497,518,519,520,597,914
- `collect_aerial2/aerial2_index.csv` で各番号のファイル名を引き、`collect_aerial2/img/{file}` を **Read で実際に読み込んで分析**する。
- 参考に `gen_event_v21/event_index_v21.csv` `gen_event_v22/event_index_v22.csv` の過去の原因シナリオも読んでよいが、**最終判断は元画像そのものを見て**原因・姿勢・状況を判断し直す。

## ■キー / 生成API（.env から読む・値出力禁止・Geminiのみ）
- `.env` の `GEMINI_API_KEY` を読む（既存 gen_event_v22.mjs の readEnv 方式）。
- Google: `POST .../v1beta/models/gemini-3-pro-image-preview:generateContent`、`contents.parts=[{inlineData:{mimeType, data:base64(元画像)}},{text:専用プロンプト}]`、`responseModalities:["IMAGE"]`、429/5xx は指数バックオフ。
- 生成スクリプトは `gen_event_v22.mjs` を雛形に**新ファイル名**（例 `gen_event_v23.mjs`）。**プロンプトは prompts_v23/{番号}.txt から読み込む**こと（汎用文字列をスクリプトに焼き込まない）。OpenAI 経路は使わない・v22スクリプトは上書きしない。

## ■作業フロー（1元絵 = 1 BACKLOG行・丁寧に）
1. 元画像を Read で分析 → 原因/姿勢/状況/機械種別を言語化。
2. 専用プロンプト（日英）を `prompts_v23/{番号}.txt` に保存。
3. その専用プロンプト＋参照画像（元絵）で Gemini 2枚生成 → `gen_event_v23/{番号}/google_1.png` `google_2.png`。
4. 自己点検（①元絵の原因/姿勢/状況に忠実か ②原因が分かる事故の瞬間か ③物理整合 ④展示会場屋内・シザース式・トラス支柱・コードカバー置換 ⑤矢印/文字なし・流血/ロゴなし ⑥タッチ統一）。①〜③が弱ければ専用プロンプトを調整し**1回だけ再生成**（各最大2回試行）。
5. `gen_event_v23/event_index_v23.csv` に〔元番号, 元ファイル, 元絵から読み取った事故原因/姿勢/状況, 専用プロンプト要約, google_1, google_2〕を記録。

## ■作業の進め方（ランナーが1タスクずつ起動）
- `BACKLOG_EVENT23.md` の未完最上段 `- [ ]` を**1つだけ**実行 → 完了で `- [x]` にして git push。
- 着手前に `gen_event_v23/{番号}/` を確認し未生成分のみ。未完が無ければ空ファイル `DONE_EVENT23.flag` を作成し停止。
- usage/rate/429 到達時は WIP を保存・push して停止（ランナーが reset 時刻まで待ち自動再開）。

## ■対比PDF / 発行
- `compare_event_v23.pdf`：1元絵1ページ・見出し＝「元絵から読み取った原因・姿勢」。左に元イラスト、右に生成2枚（Google-1/Google-2）を大きくラベル付きで並べる。元絵への忠実性と原因の分かりやすさを人が判定できる大きさ・歪み/見切れなし。
- `review_event23/<ランダム32文字>/` に PDF＋index.html（静的・外部読込なし・noindex・スマホ縦・「開く/保存」＋iframe縦表示）。
- Vercel hakuten-review を**過去全トークンの union** で再デプロイ（既存URL全200保持・ルート/=404・HTTP200を curl 実測検証）。GitHub(master/main) へ push。
- `REPORT_EVENT23.md`：公開URL／PDF直URL＋raw／生成枚数／各番号の「元絵から読み取った原因・姿勢」＋専用プロンプト要約一覧／忠実性が弱く再生成した番号／残課題 を記載し push。
