# RULES_PPT — 博展向け 事故事例PowerPoint（17事例×2スライド）作成・自律QA（全自動・無人継続）

## ■安全（厳守）
- 削除/上書き禁止（追加・新ファイル名）。**元テンプレ/既存pptxは絶対に上書きしない**。Chrome kill厳禁（taskkill厳禁）。今回 **APIキーは使わない・値も出力しない**。
- 時刻は `powershell.exe -Command "Get-Date"`。python は `py`（3.12。`python`はストアスタブで不可）。
- push は origin main。`master` へ commit→push、続けて `master:main` を fast-forward。競合時は pull --rebase 後に再push。
- 着手前に既存（cases/・hakuten_jirei_cases*.pptx・review_ppt/）を確認し、未完分のみ進める（再開で重複/破壊しない）。

## ■元テンプレ（博展フォーマット）＝既存資産を踏襲（新規レイアウトを勝手に作らない）
博展フォーマットは本リポジトリに確立済み。これを「テンプレ」として踏襲する（“テンプレ未発見”で停止しない）。S0 で下記を `template_spec.md` に解析・固定する：
- 版面参照：`hakuten_jirei_photo_v10.pptx`（17スライド・16:9＝13.333×7.5in・上に事故写真／下に項目別の体裁）。
- 再現値（実測・メモ由来）：16:9。和文=游ゴシック（タイトル太字・黒・約27-28pt「重大事故概要 〇〇」）。配色＝本文黒000000／強調・危険矢印 赤FF0000／注意三角 黄FFFF00／表ラベル列 グレーD9D9D9／フッター灰5E5E5E。
- フッター：左下「©Hakuten Corporation All Rights Reserved.」(灰~9-10pt)、右下にカラードット列＋「HÅKUTEN」ロゴ（資産 `images/ref/footer_strip_raw.png` 520x52）、右端ページ番号。
- 表（左ラベル固定・各事例[概要＋表]の2枚構成）：発覚日時／プロジェクト名／発生事象／原因概要／対応／対策概要。
- 生成補助：`build_pptx.py`（python-pptx の実装が既にある。複製して新名で使う）。
- 版面・項目位置・配色を踏襲し、**新規 `hakuten_jirei_cases.pptx`** を生成（テンプレ非破壊）。

## ■2スライド1セットの内容（17事例）
- 上＝事故写真欄：その事例の4枚（`photos_v15/{番号}/A_openai.png` `B_google.png` `C_openai_event.png` `D_google_event.png`）を **2×2 で A/B/C/D ラベル付き**配置（選抜前である旨が分かる体裁）。歪み/重なり/見切れ無く綺麗に。
- 下＝項目別記載：発覚日時／発生事象／原因概要／対応／対策概要。
- 各スライドに監修：**金田義太（労働安全コンサルタント 登録第4840号）** を入れる。

## ■対象17番号と機序（collect2/img_index.csv のカテゴリ・事故種類を参照）
0001,0002,0003,0017,0019（TGL）／0040,0041,0042,0043,0044,0046,0050,0052,0054,0057,0060,0071（高所）。

## 【最重要】出典の真正性（捏造・取り違え厳禁）
- 付与する厚労省URLは**実在し本文がその事故機序に合致するものだけ**。**URLは必ず web_fetch/curl で200かつ本文一致を確認**してから採用。確認できないURL・日時・出典は採用しない。
- 第一情報源＝職場のあんぜんサイト「労働災害事例」個別ページ（`anzeninfo.mhlw.go.jp` 配下。発生状況/原因/対策/業種/起因物が載るURL付きページ）。日時はページ記載の精度で（年・年月など。無ければ「年のみ」等と明記し精度を偽らない）。
- **TGL事例は本物のテールゲートリフター/昇降板の災害のみ**採用。ゴミ収集車・トラッククレーン・フォークリフト・タンク車・ただの荷台/ダンプ転落は**採用禁止**（過去に取り違えた類型）。**高所は起因物=高所作業車の事例のみ**。
- 機序に合致する公的事例が見つからない場合は**捏造せず**、発覚日時/URLを「公的事例 該当なし（要確認）」と明記、発生事象/原因は「AI整理（公的出典なし）」と注記して記載、対策のみ厚労省の一般資料から出典付きで挙げる。該当なし事例は報告に一覧化。
- 厚労省本文は**逐語コピーせず要約・平易化**して引用元URLを併記（丸写し回避・出典明記）。
- 各事例の照合結果は `cases/{番号}.md` に：採用URL（200確認済）／発覚日時と精度／発生事象（公的採用 or AI整理注記＋URL）／原因概要（同）／想定対応／対策（複数・各出典URL）／TGL本物確認・高所起因物確認のチェック。`data_xlsx/accidents_TGL.xlsx`・`accidents_AERIAL.xlsx` は日時の裏取り等に活用可（ただしURLはあんぜんサイト個別ページで取る）。

## ■成果物（新ファイル名・テンプレ非破壊）
- `hakuten_jirei_cases.pptx`（テンプレ複製で表紙＋17セット=34スライド、本文流し込み・4枚2×2配置・監修表記）。

## ■自律QAループ（客先提供のプロ水準・停止条件つき）
- pptx→PDF/PNG変換して**実際に目視点検**。変換は LibreOffice 不在のため **PowerPoint COM**（`render_pptx.ps1` 方式＝Presentation を開き Slide.Export でPNG、または ExportAsFixedFormat でPDF）を使う。Chrome は触らない。PDFが出来れば `py -c "import fitz"`(PyMuPDF)でPNG化して目視。
- 観点①テンプレ版面に忠実 ②4枚が歪み/重なり/見切れ無く綺麗 ③文字の溢れ/見切れ/改行崩れ無し ④全項目が埋まっている ⑤日本語がプロ資料として自然・敬体統一 ⑥フォント/余白/整列/配色が一貫 ⑦出典URLが実在・200・機序一致（再検証）⑧TGLが本物TGLか再確認 ⑨対策に出典 ⑩監修表記 ⑪捏造データ無し ⑫客先にそのまま出せる体裁か。
- 各ラウンドで採点し、問題は `QA_ISSUES_r{n}.md` に列挙→修正→再変換→再点検。**問題ゼロで完了、最大6ラウンドで打ち切り**（残課題は正直に報告）。修正は新コミットで積み、各ラウンド後に git push。

## ■公開・報告
- 最終pptxをPDF化（スマホ閲覧用）。`review_ppt/<ランダム32文字>/` に PDF＋pptx＋index.html(静的・外部読込なし・noindex・スマホ縦・PDFを開く/保存＋pptxダウンロード＋iframe縦表示) を置き Vercel hakuten-review に再デプロイ（既存URL保持・ルート/=404・HTTP200検証）。pptxとPDFを GitHub(main) へ push。
- `REPORT_PPT.md`：公開URL/PDF直URL/pptxDL/raw・事例別（採用URL／日時精度／発生事象・原因が「公的採用」か「AI整理(公的出典なし)」か／対策の出典数）・公的該当なし事例一覧・QAラウンド数/各指摘数/残課題・HTTP・非破壊/捏造なし/TGL本物のみ の確認 を記載し push。
