# RULES_PROP — 商談用 提案資料 proposal_hakuten 全自動作成（メラビアン重視・ビジュアル7割）

## ■安全（厳守）
- 削除/上書き禁止（追加・新ファイル名のみ）。**既存成果物/テンプレは非破壊**（読むだけ・再利用のみ）。
- `rm`/`Remove-Item`/`del`/`move`/`taskkill` 禁止。**Chrome kill 厳禁**。稼働中のランナーは止めない。
- **今回は新規画像生成をしない**（既存の手元データ・既存生成画像・既存図表のみ使用）。OpenAI/Gemini 画像APIは呼ばない。
- APIキー値は出力しない（必要なら .env から読むのみ・末尾マスク）。
- 時刻は `powershell.exe -Command "Get-Date"`。python は `py`（3.12）。
- push は origin main（master へ commit→push、続けて master:main を fast-forward。競合時 pull --rebase 後再push）。
- **捏造禁止**：確認できない数値・法令・日付・出典は載せない。下記「確定素材」以外の数値を作らない。
- 制限到達（usage/rate/429）時は WIP を保存・push して停止（ランナーが自動再開）。
- 着手前に `figs/`・`proposal_hakuten.pptx`・`qa_prop/`・`review_prop/` の既存を確認し未完分のみ進める（再開安全）。

## ■資料の絶対方針（メラビアンの法則＝ビジュアル7割・文字最小）
- **1スライド＝1メッセージ**。文字は最小（各スライド要点 最大3行・1行20字目安）。グラフ・アイコン・大きな数字が主役。
- 「読ませる」でなく「見て伝わる」。長文・箇条書きの羅列は禁止。大きな数字＋短いキャプション＋図。
- 配色は**安全色**（赤=危険／黄=注意／緑=対策）を効果的に。視覚的に締まったデザイン。
- **日本語フォント必須**（游ゴシック等。matplotlibグラフも日本語フォント設定し文字化け回避）。
- 16:9（幅13.333in×高さ7.5in）。python-pptx で生成。

## ■使う既存データ（手元の実数・捏造しない＝確定素材）
- 走査規模：**約42万件**（死亡DB 1991-2018 ＋ 死傷DB 2006-2017）から抽出。
- **TGL（計1,878）**：死亡121／死傷1,757。型トップ3＝①はさまれ581(30.9%)②墜落457(24.3%)③激突され165。死亡のみ(n=121)は①はさまれ64(52.9%)が過半。
- **高所（計1,149）**：死亡358／死傷791。型トップ3＝①墜落392(34.1%)②はさまれ281(24.5%)③転倒84。**高所は死亡率が高い**。
- 既存グラフ（流用優先）：`figs/fig_tgl_type_rank.png`（TGL型別）・`figs/fig_aerial_type_rank.png`（高所型別）・`figs/fig_death_vs_injury.png`（死亡vs死傷）・`figs/fig_tgl_timeline.png`（TGL法制化）・`figs/fig_qual_table.png`（資格早見）・`figs/fig_danger_points.png`（危険ポイント）。
  - 既存figで足りない図（科学的安全の流れ図・自動化の対比図＝人手×日→AI×時間・3アイコン等）は **matplotlib で新規生成**（日本語フォント・安全色・新ファイル名 `figs/prop_*.png`）。捏造数値は使わず、概念図・既存確定値の可視化に留める。
- 法令の確定素材（オーナー検証済・改変禁止）：
  - TGL特別教育義務化＝**令和6年(2024)2月1日施行**（安衛則第36条・特別教育を要する業務に追加）、罰則**6月以下の懲役or50万円以下**、**学科4h＋実技2h**。
  - 高所作業車＝作業床**10m以上は技能講習／未満は特別教育**、**フルハーネス6.75m超で着用義務**、安衛則第194条の22。
  - 出典は参考として小さく添える。

## ■指定フォーマット事故事例（slide 9 用・既存流用）
- 博展テンプレ（`template_spec.md`：上＝事故写真／下＝項目別記載）に沿う。
- 本文＝`cases_v2/N01.md`〜`N15.md`（発生事象／原因概要／対応／対策概要＝箇条書き）。画像＝`photos_v16/N01/{base,openai,google}.png`。
- 1〜2例を流し込む。**出典は参考資料として小さく**。**監修表記**（監修：金田 義太 労働安全コンサルタント 登録第4840号）。**「AI再現イメージ」である旨を小さく明記**。

## ■スライド構成（10枚前後・ビジュアル主体）
1. 表紙：「データに基づく安全管理 ＋ AIによる自動化」／株式会社 博展 御中／監修：金田 義太（労働安全コンサルタント 登録第4840号）／サブ1行「経験と勘に頼らない、科学的な安全教育を、速く・安く。」
2. 課題提起：従来の安全教育は“ベテランの経験頼み”。属人的・再現性がない。大きな図1点＋短句。
3. 解決アプローチ：「事故データ分析 → 危険を科学的に特定 → 教材化 → AIで量産」を矢印図1枚で（文字最小）。
4. データ分析①：約42万件の規模感を大きな数字で＋TGL/高所 型別ランキング横棒。「TGL＝はさまれ最多／高所＝墜落最多」を一言。
5. データ分析②：死亡vs死傷の対比。「高所は致死率が高い」「TGLは死亡の半分以上がはさまれ」を大きな数字で。
6. 科学的対策：データから導く対策をアイコン＋短句（墜落/はさまれ/感電 等）。法令根拠は小さく。
7. 自動化の価値（売り）：「AIで事故事例教材を量産」。ビフォー/アフター・人手○日→AI○時間の対比、属人化しない・更新が速い・低コストを3アイコンで。
8. 成果物サンプル：これまで作った事例集/学習資料の見た目を縮小し「こういうものが作れます」（AI再現イメージ注記・小さく）。
9. 指定フォーマット事故事例（1〜2枚）：博展テンプレに既存画像＋cases_v2本文を流し込み。出典小さく・監修表記・AI注記。
10. まとめ＋提案：「科学的安全 × 自動化」を1枚で。連絡先/監修者名。押し売りでなく価値を1行で。

## ■成果物
- `proposal_hakuten.pptx`（新規・ビジュアル主体・文字最小・10枚前後）。
- `proposal_hakuten.pdf`（PowerPoint COM で変換＝LibreOffice不在・Chrome不可侵。スマホ閲覧用）。

## ■自律QA（プロ水準まで・最大4ラウンド／実際にPNGレンダして目視）
- 各ラウンド pptx→PDF→PNG 化して**実際に目視点検**（自己採点だけにしない・`qa_prop/` に保存）。
- 観点：①1スライド1メッセージで文字が少ないか（多ければ削る）②グラフ/図が主役で見やすいか③数字が大きく伝わるか④安全色で締まっているか⑤文字溢れ・改行崩れ・画像歪み無し⑥指定フォーマット事例が体裁どおり⑦監修表記⑧AI再現イメージ注記⑨捏造数値/法令無し⑩明日そのまま見せられる完成度。
- 問題ゼロ or 最大4ラウンドで打ち切り。修正は新コミットで積み push。

## ■公開・報告
- `review_prop/<ランダム32文字>/` に pptx＋PDF＋index.html（静的・外部読込なし・noindex・スマホ縦・pptxダウンロード＋PDFを開く/保存＋iframe縦表示）を置く。
- Vercel **hakuten-review** 再デプロイ（`vercel deploy --prod`）。**過去の全レビュートークンをunionで束ね既存URLを全て200保持**（既存の `review_layer_v19/.vercel` のプロジェクトを利用し、全 `review_*/<32文字>` 既存トークンディレクトリ＋新トークンを同梱）。ルート `/`=404・HTTP200 を curl で実測検証。
- pptx/PDF/index.html を GitHub(main) push。
- `REPORT_PROP.md`：公開URL／pptx・PDF直URL＋raw／スライド数／QAラウンド数／残課題 を記載し push（キー値非出力・新規画像生成なしを明記）。
