# RULES_EVENT21 — 展示会設営 高所作業車 事故「原因」が見える安全教育イラスト（やり直し版・Geminiのみ・全自動・無人継続）

## ■目的 / 前回(event_v20)の失敗
- 前回は「人が墜落している最中」の結果だけを描き、なぜ事故が起きたか（不安全行動・不安全状態＝原因）が画面に無く、安全教育資料にならなかった→不採用。
- 今回は各イラストに「事故の引き金になった原因」を画面内に明確に描く。見た人が「これが原因だ／これが危なかった」と分かる一枚にする。**イラストのみ**。

## ■【最重要】事故の「原因」が見える絵にする
- 「転落・転倒の瞬間」と「その原因（不安全行動・不安全状態）」を**同じ画面に共存**させる。単なる落下中の絵にしない。
- 原因の例（事故の型に応じて1つ設定し、それが画面に見えるように描く）:
  ・段差/床の隙間/ケーブルを踏んで機体が傾いた → 傾いた機体と、その下の段差/隙間/ケーブルが見える
  ・バスケットの手すりに足をかけて/手すりに乗って無理な姿勢 → 手すりに足を乗せる動作が見える
  ・身を乗り出しすぎ/構造物へ乗り移ろうとした → 乗り出す・乗り移る動作が見える
  ・アウトリガー未展張/接地不良 → 張られていないアウトリガー・浮いた脚が見える
  ・過積載/ブームの無理な伸ばし → 過積載・無理な姿勢が見える
  ・フルハーネスのフック未接続 → 掛かっていないフックが見える

## ■安全（厳守）
- 削除/上書き禁止（追加は必ず新ファイル名）。既存成果物（collect_aerial2/・gen_event_v20/・review_* 等）は非破壊。
- Chrome を kill しない。taskkill しない。rm/Remove-Item/del/move を使わない。
- **OpenAI は使わない**（今回は Google のみ）。
- APIキー/認証値は **値を出力しない**（`.env` から読む。出す場合は末尾4桁マスクのみ）。
- 時刻は `powershell.exe -Command "Get-Date"`。git push は origin master(=main) に append-only。競合は `git pull --rebase` 後再push。
- 着手前に `gen_event_v21/` の既存を確認し**未生成分のみ**（出力先が既にあれば skip＝再開安全）。捏造禁止。

## ■キー / 生成API（.env から読む・値出力禁止・Geminiのみ）
- `.env` の `GEMINI_API_KEY` を読む（既存 gen_event_v20.mjs / gen_img.mjs の readEnv 方式）。
- Google: `POST .../v1beta/models/gemini-3-pro-image-preview:generateContent`、
  `contents.parts=[{inlineData:{mimeType, data:base64(元画像)}},{text:プロンプト}]`、`responseModalities:["IMAGE"]`、429/5xx は指数バックオフ。
- 既存 `gen_event_v20.mjs` を雛形に**新ファイル名**（例 `gen_event_v21.mjs`）で作る（v20スクリプトは上書きしない）。OpenAI 経路は使わない。

## ■元絵25番号（前回と同じ・collect_aerial2/img/ 参照）
1,2,3,45,64,91,93,94,97,100,104,112,114,137,138,196,215,372,379,497,518,519,520,597,914
- `collect_aerial2/aerial2_index.csv` で各番号のファイル名・事故の型を引き、**事故の型を踏襲**する。
- 元絵の構図をなぞらず、その事故の型に「ありがちな原因シナリオ」を1つ設定し、その原因が見える展示会設営シーンとして描く。
- 設定した原因シナリオを index に記録（後で人が分かるように）。元画像は参照画像として渡す（型・状況の手がかり用）。

## ■統一ルール
- 高所作業車は登場時すべて**シザース型（垂直昇降式・シザースリフト）**に統一。
- タッチ統一：高品質・臨場感・リアル寄りの安全教育用イラスト（陰影・立体感のあるクリーンな作画）。元絵の素朴さに引きずられない。
- 舞台＝日本の展示会場・ブース設営現場（組みかけブース、トラス、看板パネル、什器、搬入口、床の配線・段差等）。人物＝日本人作業員、あごひもヘルメット・蛍光ベスト・安全靴・フルハーネス。
- 事故の瞬間＋その原因が共に分かること。画像内テキスト一切なし（文字/矢印/キャプション/吹き出し/記号なし）。流血なし。実在ロゴ/ブランドなし。

## ■生成枚数（イラストのみ・Geminiで2案）
- 各番号 Gemini で2枚（原因シナリオに沿ったバリエーション2案）＝25×2＝**50枚**。
- 生成後に自己点検（①事故の"原因"が画面に描かれているか＝最重要 ②シザース型か ③高品質統一タッチか ④文字/矢印無し ⑤PPE適切・流血/ロゴ無し）。
- ①が満たせない（ただの落下中になっている）場合は、原因が見えるよう指示を強めて**1回だけ再生成**（各最大2回試行）。

## ■保存 / index
- `gen_event_v21/{番号}/google_1.png` `google_2.png`（通常PNG）。
- `gen_event_v21/event_index_v21.csv`：〔元番号, 元ファイル, 元の事故の型, 設定した原因シナリオ, 生成タイトル, google_1, google_2〕。

## ■作業の進め方（ランナーが1タスクずつ起動）
- `BACKLOG_EVENT21.md` の未完最上段 `- [ ]` を**1つだけ**実行 → 完了で `- [x]` にして git push。
- 着手前に `gen_event_v21/{番号}/` を確認し未生成分のみ。未完が無ければ空ファイル `DONE_EVENT21.flag` を作成し停止。
- usage/rate/429 到達時は WIP を保存・push して停止（ランナーが reset 時刻まで待ち自動再開）。

## ■対比PDF / 発行
- `compare_event_v21.pdf`：1元絵1ページ・見出し＝元番号＋事故の型＋設定した原因シナリオ。左に元イラスト（出所ドメイン付）、右に生成2枚（Google-1/Google-2）を大きくラベル付きで並べる。原因が描けているか人が判定できる大きさ・歪み/見切れなし。
- `review_event21/<ランダム32文字>/` に PDF＋index.html（静的・外部読込なし・noindex・スマホ縦・「開く/保存」＋iframe縦表示）。
- Vercel hakuten-review を**過去全トークンの union** で再デプロイ（既存URL全200保持・ルート/=404・HTTP200を curl 実測検証）。GitHub(master/main) へ push。
- `REPORT_EVENT21.md`：公開URL／PDF直URL＋raw／生成枚数／各番号の原因シナリオ一覧／原因描写が弱く再生成した番号／残課題 を記載し push。
