# collect_plan.md — 事故イラスト収集(A) ＋ 事故情報1000件×2 Excel化(B) 計画書

作成: 2026-06-14 18:50 / 担当: 無人ランナー（RULES_C.md 準拠・収集のみ・画像生成なし・append-only）

---

## 0. 前提・安全（RULES_C.md 再掲）
- 削除/上書き禁止。追加は新ファイル名のみ。`rm/Remove-Item/del/move/taskkill` 不使用。Chrome killしない。
- APIキー/認証情報の値は出力しない。**本フェーズは収集のみ（画像生成なし）**。
- 着手前に既存（`collect2/`・`data_xlsx/`・`data/jniosh/`）を確認し、未取得分のみ追加。再開で重複/破壊しない。
- ブラウザ画像検索は instagram-automation 方式・別プロファイル・Chrome kill無し。
- push は origin main(=master) に append-only。競合は `git pull --rebase` 後に再push。

## 1. 既存状況（2026-06-14 18:50 時点で確認）
- `data/jniosh/`: SHIBO_2014〜2018.csv（死亡災害DB 5年分）が**既に存在** → B1 で流用、未取得年のみ追加。
- `collect2/`: 空（イラスト未収集） → A2以降で新規連番保存。
- `data_xlsx/`: 空（Excel未生成） → B5で生成。
- `collect_plan.md`: 本ファイルで新規作成（A1）。

---

## (A) 事故イラスト 50〜100枚

### A-1 対象
TGL付きトラック（テールゲートリフター/パワーゲート/昇降板）および高所作業車（ブーム式/垂直昇降式/作業床/バスケット）の
**「事故イラスト・事故が写った図/写真」**。事故種類は不問（墜落・転落/転倒/滑落/下敷き/はさまれ/巻き込まれ/感電/逸走/激突/飛来落下 等）。

### A-2 除外
- 通常作業のみ（事故が起きていない）／構造・部品名称図／寸法図／無関係な単体機械写真。

### A-3 収集先（幅広く）
- 陸災防(陸上貨物運送事業労働災害防止協会)、建災防、中災防、林災防
- 各都道府県労働局・労基署のリーフレット/PDF
- KYT教材・危険予知トレーニング素材、災害事例イラスト集
- メーカー取説の災害・警告図（テールゲートリフター/高所作業車）
- 安全ポスター、各種安全衛生サイト、安全教育サイト

### A-4 重複排除・保存
- md5（可能ならpHash併用）で重複排除。実体が同じものは1枚に統合。
- 保存: `collect2/img/0001.jpg …` 連番(4桁・通し番号)。
- 索引: `collect2/img_index.csv` 列＝〔通し番号, ファイル名, カテゴリ(TGL/高所), 事故種類, 出所URL, md5, 1行説明〕。
- バッチ構成: A2(TGL b1)→A3(TGL b2)→A4(高所 b1)→A5(高所 b2)→A6(全体再排除・通し番号確定)。最低50、可能なら100近く。

---

## (B) 事故情報 各1000件（TGL群 / 高所群）

### B-1 一次データ源
1. **JNIOSH整形CSV**: https://www.jniosh.johas.go.jp/publication/houkoku/houkoku_2022_01.html
   （死亡災害DB H3-H30 / 死傷DB H18-H29）をDLし `data/jniosh/` 配下に保存→grep。
   既存 SHIBO_2014〜2018.csv は流用、未取得年・死傷DBを追加取得。
2. **あんぜんサイト**: 死亡災害DB( https://anzeninfo.mhlw.go.jp/anzen_pg/SIB_FND.html ) /
   死傷DB( https://anzeninfo.mhlw.go.jp/anzen_pgm/SHISYO_FND.html ) を起因物・事故の型で絞り込み。
3. 出典URLの付く公的事故報告。

### B-2 抽出ルール
- **TGL群**: 起因物/災害発生状況に「テールゲートリフター/パワーゲート/昇降板付きトラック」等が該当。
- **高所群**: 「高所作業車/ブーム/作業床/バスケット（起因物コード146 等）」が該当。
- 各群1000件目標。集まらなければ最大数を集め件数を報告。

### B-3 重複排除・分類
- 重複キー: (発生年月日 + 業種 + 事故の型 + 災害発生状況) のハッシュ。
- カテゴリ（事故の型）: 墜落・転落 / 転倒 / はさまれ・巻き込まれ / 崩壊・倒壊 / 飛来・落下 / 激突 / 感電 等。

### B-4 出力（Excel）
- `data_xlsx/accidents_TGL.xlsx` と `data_xlsx/accidents_AERIAL.xlsx`
  （または1ブック2シート＋集計シート）。
- 列＝〔通し番号, カテゴリ(事故の型), 発生年, 業種, 起因物, 災害発生状況(全文), 出典(ファイル名+行番号 or URL), データ源〕。
- **出典URLは全件必須**。無いものは除外。

---

## (C) 最終発行
- `img_catalog.pdf`（全イラストを通し番号付き一覧＝後から選べる）作成。
- `review_collect/<ランダム32文字>/` に PDF＋index.html(静的・外部読込なし・noindex・スマホ縦)＋Excel を配置し
  Vercel hakuten-review へ再デプロイ（既存URL保持・ルート/=404・HTTP200検証）。成果物を GitHub(main)へ push。
- `REPORT_C.md` に〔公開URL/PDF/Excel直URL＋raw・イラスト総数・事故件数(TGL/高所)・カテゴリ別件数〕を記載し push。

---

## 実行順（BACKLOG_C.md にマップ）
A1(本計画) → A2 → A3 → A4 → A5 → A6 → B1 → B2 → B3 → B4 → B5 → C1 → C2 → C3
各タスク完了で `- [x]` 化し commit/push。制限到達時はWIP保存・push して停止（ランナー再開）。
