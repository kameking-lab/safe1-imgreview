# RULES_EVENT22 — 展示会設営 高所作業車「危険姿勢→事故の流れを矢印で図示」KYT安全教育イラスト（Geminiのみ・全自動・無人継続）

## ■目的
1枚のイラストの中で、(1)作業員が不安全な姿勢・状況で作業している様子 と (2)そこからどう動いて事故になるか（転落・転倒・挟まれの方向）を、**矢印**で示す KYT（危険予知）教材図を作る。**イラストのみ**。

## ■【最重要】危険姿勢→事故への流れを「矢印」で図示する
- (1)危険な作業姿勢・状況を主に描き、(2)そこに「作業員の動き／重心の移動／転落・転倒・挟まれの方向」を表す矢印を加える（KYT教材・ヒヤリハット図でよくある予測矢印）。
- 矢印は赤やオレンジの分かりやすい湾曲矢印。静止画だが「この姿勢のままだとこう事故になる」流れが矢印で読めること＝最重要。
- 完全に落下しきった絵にしない（これから事故になる流れを見せる）。
- 例: 番号2＝手すりに足を掛け身を乗り出して作業 → 重心が外へ → 転落方向の矢印。番号497＝上昇したまま走行し段差に片輪 → 傾く方向の矢印で横転。番号914＝真下に作業員がいるのに下降操作 → 挟まれる方向の矢印。
- 文字・キャプション・吹き出しは入れない（**矢印のみ可**）。流血なし。実在ロゴ/ブランドなし。

## ■安全（厳守）
- 削除/上書き禁止（追加は必ず新ファイル名）。既存成果物（collect_aerial2/・gen_event_v20/・gen_event_v21/・review_* 等）は非破壊。
- Chrome を kill しない。taskkill しない。rm/Remove-Item/del/move を使わない。
- **OpenAI は使わない**（Google gemini-3-pro-image-preview のみ）。
- APIキー/認証値は **値を出力しない**（`.env` から読む。出す場合は末尾4桁マスクのみ）。
- 時刻は `powershell.exe -Command "Get-Date"`。git push は origin master(=main) に append-only。競合は `git pull --rebase` 後再push。
- 着手前に `gen_event_v22/` の既存を確認し**未生成分のみ**（出力先が既にあれば skip＝再開安全）。捏造禁止。

## ■キー / 生成API（.env から読む・値出力禁止・Geminiのみ）
- `.env` の `GEMINI_API_KEY` を読む（既存 gen_event_v21.mjs の readEnv 方式）。
- Google: `POST .../v1beta/models/gemini-3-pro-image-preview:generateContent`、
  `contents.parts=[{inlineData:{mimeType, data:base64(元画像)}},{text:プロンプト}]`、`responseModalities:["IMAGE"]`、429/5xx は指数バックオフ。
- 既存 `gen_event_v21.mjs` を雛形に**新ファイル名**（例 `gen_event_v22.mjs`）で作る（v21スクリプトは上書きしない）。OpenAI 経路は使わない。

## ■元絵25番号 / シナリオ継承
1,2,3,45,64,91,93,94,97,100,104,112,114,137,138,196,215,372,379,497,518,519,520,597,914
- `gen_event_v21/event_index_v21.csv` から各番号の「元の事故の型」「設定した原因シナリオ」を読み込み、それを「危険姿勢で作業 → 矢印で事故への遷移」の構図に作り直す。
- 元絵（collect_aerial2/aerial2_index.csv で番号→ファイル名）は参照画像として渡す（事故の型・状況の手がかり用）。
- 各番号の「危険姿勢の内容」と「矢印が示す事故への流れ」を index に記録。

## ■統一ルール
- 高所作業車は登場時すべて**シザース型（垂直昇降式）**に統一。
- タッチ統一：高品質・臨場感・リアル寄りの安全教育用イラスト（陰影・立体感のあるクリーンな作画）。
- 舞台＝日本の展示会場・ブース設営現場（組みかけブース、トラス、看板パネル、什器、搬入口、床の配線・段差等）。人物＝日本人作業員、あごひもヘルメット・蛍光ベスト・安全靴・フルハーネス。
- 「危険姿勢で作業している状態」を主に描き、そこに「事故への遷移を示す矢印」を加える。

## ■生成枚数（イラストのみ・Geminiで2案）
- 各番号 Gemini で2枚（矢印表現のバリエーション2案）＝25×2＝**50枚**。
- 生成後に自己点検（①危険な作業姿勢が描けているか ②事故への遷移を示す矢印があるか＝最重要 ③シザース型か ④統一タッチか ⑤PPE適切・流血/ロゴ/余計な文字なし）。
- 矢印が無い/不明瞭なら指示を強めて**1回だけ再生成**（各最大2回試行）。

## ■保存 / index
- `gen_event_v22/{番号}/google_1.png` `google_2.png`（通常PNG）。
- `gen_event_v22/event_index_v22.csv`：〔元番号, 元の事故の型, 危険姿勢の内容, 矢印が示す事故への流れ, google_1, google_2〕。

## ■作業の進め方（ランナーが1タスクずつ起動）
- `BACKLOG_EVENT22.md` の未完最上段 `- [ ]` を**1つだけ**実行 → 完了で `- [x]` にして git push。
- 着手前に `gen_event_v22/{番号}/` を確認し未生成分のみ。未完が無ければ空ファイル `DONE_EVENT22.flag` を作成し停止。
- usage/rate/429 到達時は WIP を保存・push して停止（ランナーが reset 時刻まで待ち自動再開）。

## ■対比PDF / 発行
- `compare_event_v22.pdf`：1元絵1ページ・見出し＝「危険姿勢→事故」シナリオ。左に元イラスト、右に生成2枚（Google-1/Google-2）を大きくラベル付きで並べる。矢印で流れが読めるか判定できる大きさ・歪み/見切れなし。
- `review_event22/<ランダム32文字>/` に PDF＋index.html（静的・外部読込なし・noindex・スマホ縦・「開く/保存」＋iframe縦表示）。
- Vercel hakuten-review を**過去全トークンの union** で再デプロイ（既存URL全200保持・ルート/=404・HTTP200を curl 実測検証）。GitHub(master/main) へ push。
- `REPORT_EVENT22.md`：公開URL／PDF直URL＋raw／生成枚数／各番号の「危険姿勢→事故」シナリオ一覧／矢印が弱く再生成した番号／残課題 を記載し push。
